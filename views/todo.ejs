<!DOCTYPE html>
<html lang="en">
<head>
<title>Todo List — Modern UI</title>

<style>
/* ===== GLOBAL ===== */
*{
  box-sizing:border-box;
  margin:0;
  padding:0;
  font-family: "Poppins", sans-serif;
}

body{
  min-height:100vh;
  background:linear-gradient(135deg,#667eea,#764ba2);
  display:flex;
  align-items:center;
  justify-content:center;
  padding:40px;
  color:#fff;
}

/* ===== MAIN CARD ===== */
.container{
  width:100%;
  max-width:700px;
  backdrop-filter: blur(20px);
  background:rgba(255,255,255,0.1);
  border-radius:20px;
  padding:30px;
  box-shadow:0 20px 40px rgba(0,0,0,0.3);
}

/* ===== TITLE ===== */
h1{
  text-align:center;
  margin-bottom:25px;
  font-size:26px;
  font-weight:600;
}

/* ===== TODO LIST ===== */
ul{
  list-style:none;
}

ul li{
  background:rgba(255,255,255,0.15);
  padding:14px 16px;
  border-radius:12px;
  margin-bottom:12px;
  position:relative;
  transition:.3s;
  backdrop-filter:blur(10px);
}

ul li:hover{
  transform:translateY(-2px) scale(1.01);
  background:rgba(255,255,255,0.25);
}

/* ===== BUTTONS ===== */
.delete-btn,
.edit-btn{
  position:absolute;
  top:10px;
  font-size:18px;
  text-decoration:none;
  transition:.2s;
}

.delete-btn{
  right:10px;
  color:#ff4d4d;
}

.edit-btn{
  right:40px;
  color:#ffd54f;
}

.delete-btn:hover{
  transform:scale(1.2);
}

.edit-btn:hover{
  transform:scale(1.2);
}

/* ===== FORM ===== */
form{
  margin-top:25px;
  display:flex;
  gap:10px;
}

input[type="text"]{
  flex:1;
  padding:12px;
  border-radius:10px;
  border:none;
  outline:none;
  font-size:15px;
}

input[type="submit"]{
  background:#00e5ff;
  border:none;
  color:#000;
  padding:12px 18px;
  border-radius:10px;
  cursor:pointer;
  font-weight:600;
  transition:.3s;
}

input[type="submit"]:hover{
  background:#00bcd4;
  transform:translateY(-1px);
}
</style>
</head>

<body>

<div class="container">

<h1>🚀 Jenkins Learning Plan — Todo</h1>

<ul>
  <% todolist.forEach(function(todo, index) { %>
  <li>
    <a href="/todo/delete/<%= index %>" class="delete-btn">✘</a>
    <a href="/todo/<%= index %>" class="edit-btn">✎</a>
    <%- todo %>
  </li>
  <% }); %>
</ul>

<form action="/todo/add/" method="post">
  <input type="text" name="newtodo" placeholder="Add new task..." autofocus />
  <input type="submit" value="Add" />
</form>

</div>

</body>
</html>
